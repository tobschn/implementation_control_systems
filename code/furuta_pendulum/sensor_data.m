function [sd] = sensor_data(y)
    
    if( y<1 ) % this is just the skeleton, function should return a 0 or 1 depending on something having gone wrong (0) or having been successful (1).
        sd = 1;
    else
        sd = 0;
    end    
end
